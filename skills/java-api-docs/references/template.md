# {业务模块名} API 文档

- **项目约定**: [API 约定](../_conventions.md)
- **是否需要认证**: <是/否>
- **最后更新时间**: <yyyy-MM-dd>

**响应信封**（只在此处写一次；下文各接口表格只描述 `data`）：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

## 通用说明

{仅限当前控制器的通用说明, 没有省略此章节}

## 目录

{接口的目录导航}

## 接口列表

{展示接口列表, 包括接口的路径以及各种参数}

### 1. {简洁中文接口名称}

{一句话业务说明和权限码(如有), 权限码格式为 `{moduleCode}:{action}`，如 `users:create`}。

{下面为接口地址, 接口地址需要包含网关前缀, 如下`/admin`就是网关前缀}

```
GET /admin/users/info/{username}
```

**路径参数**: （无则整节省略）

| 字段名   | 类型   | 必填 | 说明   |
| -------- | ------ | ---- | ------ |
| username | string | 是   | 用户名 |

**查询参数**:（无则整节省略）

**请求体**: （无则整节省略）

`body` 为对象时展开真实字段：

| 字段名    | 类型                   | 必填 | 说明         |
| --------- | ---------------------- | ---- | ------------ |
| tableName | string                 | 是   | 主表表名     |
| data      | Record<string, object> | 是   | 主表字段与值 |

整段 `body` 不为对象（标量 / 数组等）时，字段名用 `_(body)_`：

| 字段名     | 类型     | 必填 | 说明               |
| ---------- | -------- | ---- | ------------------ |
| _(body)_   | number[] | 是   | 待提交记录 ID 列表 |

**响应体**（即信封里的 `data`）:

`data` 为对象时**必须展开真实字段**（来自 `resolve_types`）。禁止只写「返回 `UserInfo` 对象」而不给表。

| 字段名 | 类型   | 说明   |
| ------ | ------ | ------ |
| id     | string | 记录 ID |
| name   | string | 名称   |

同一 DTO 在本文多处出现时：可在文末 `## 数据模型` 写全定义，此处写「见 [UserInfo](#userinfo)」。

`data` 不为对象（标量 / 数组等）时，字段名用 `_(data)_`：

| 字段名    | 类型    | 说明     |
| --------- | ------- | -------- |
| _(data)_  | boolean | 是否成功 |

**响应成功示例**:

```json
{
  "code": 0,
  "msg": "success",
  "data": true
}
```

---

## 数据模型（可选；同 Controller 内 DTO 复用时）

### UserInfo

| 字段名   | 类型     | 说明     |
| -------- | -------- | -------- |
| userId   | string   | 用户 ID  |
| username | string   | 登录名   |
| roles    | string[] | 角色列表 |
